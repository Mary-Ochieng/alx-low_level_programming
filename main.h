# if nder MAIN_H
# define MAIN_H
# include <stdio.h>

void positive_negatinve(int);
int largest_number(int a,ant b,int c);
void print_remaining_days(int month, int day, int year);
int content_day(int month, int day);

#endif
